/// All granular permissions in the system.
enum Permission {
  createOrder,
  editOrder,
  deleteOrder,
  manageStaff,
  viewReports,
  manageExpenses,
  manageBusiness,
  accessSettings; // Added for compatibility and future use
}
